@inject Services.ICategoryService categoryService;
@using SpacedRepApp.Share;
@using SpacedRepApp.UI.Components
 
<div class="top-row pl-4 navbar navbar-dark">
    <a class="navbar-brand" href="">SpacedRepApp.UI</a>
    <button class="navbar-toggler" @onclick="ToggleNavMenu">
        <span class="navbar-toggler-icon"></span>
    </button>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <ul class="nav flex-column">
        <li class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </li>
        <CategoryMenuComponent />
        <li class="nav-item px-3">
            <NavLink class="nav-link" href="addNewCategory" Match="NavLinkMatch.All">
                <span class="oi oi-plus" aria-hidden="true"></span> New Category
            </NavLink>
        </li>
        @code {
            private bool collapseNavMenu = true;

            private string NavMenuCssClass => collapseNavMenu ? "collapse" : null;

            private void ToggleNavMenu()
            {
                collapseNavMenu = !collapseNavMenu;
            }
        }       
    </ul>
</div>
